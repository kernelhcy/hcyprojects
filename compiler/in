int main()
{	
	
	int j, m, i, n;
	bool a, b, c, d;
	
	bool con;
	//con = false;

	//j = m + (j - 3 * 5) / 2 * i;
	//m = 3;
	//n = j + m;
	
	
	if (a && (b || !c) || !con && ( j < 3 ))
	{
		j = (m + 2) * j;
	}
	
	while (con)
	{
		j = (j - 1) / n;
		//con = true;
	}
	/*
	if (c)
	{
		j = m + (j - 3 * 5) / 2 * i;
	}
	else
	{
		while (a &&(c || !b) && con)
		{
			if ( a || d && ( con && ! ( b || c)))
			{
				j = m + k;
			}
			else
			{
				j = m - k;
			}

		}
	}
	*/
	return 0;
}
